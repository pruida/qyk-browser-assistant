# 更新记录

## v0.19.5

- 外观专利检索、专利以图搜图和智慧芽任务会由启源浏览器助手接管，不再落入服务端 BrowserAct。
- 专利任务意图支持“检索专利”和“专利检索”两种词序，网页预判与扩展最终判断保持一致。

## v0.19.4

- 路由判断加入当前聊天会话记录、浏览器任务状态和上一轮澄清问题，不再只看当前几个字。
- 插件询问热点范围后，“AI科技热点”“不限领域”“第二个”等简短回答会继续原搜索任务。
- 澄清答案会与原始搜索目标合并后交给规划器；总结、翻译、写作等内容处理仍走普通对话。

## v0.19.3

- 每条消息重新判断是否需要浏览器，不再因为同一会话曾搜索过就接管所有后续输入。
- 总结、翻译、改写、润色、提炼、排序和排版等内容处理直接进入普通对话。
- “补充一下、再查、还有没有遗漏、打开第几条、继续”等明确浏览器追问仍可延续已有搜索上下文。

## v0.19.2

- 修复“总结这些文章”等具体追问仍沿用最初检索目标、继续搜索直至超时的问题。
- 只有“继续/重试”才继承上一轮目标和动作去重记录；新追问保留结果账本，但按新目标重新规划。
- 上游 SSE 长时间无数据时更快切换模型并留下诊断记录，避免一次卡顿吃满浏览器 90 秒预算。

## v0.19.1

- 修复资讯站或搜索页后台继续加载时，已完成/已暂停任务被意外重新激活的问题。
- “继续/重试”会保留上一轮的具体修正目标，并跨轮次拦截页面未变化时的重复动作。
- 规划等待与服务端硬截止对齐；Astra 返回空白或无效规划时自动回退 Sol，失败时显示真实原因。

## v0.19.0

- 首个网页改为先由 GPT-6 根据完整任务规划，再执行模型返回的跳转；不再按 Google Patents、Bing 等关键词猜测地址。
- 语音浏览器任务会播报“正在理解检索要求并规划首个网页”，规划失败安全暂停，不会回退到猜测的搜索链接。

## v0.18.1

- 修复语音“在 Google Patent 里搜彭瑞达的专利”等指令：检索词现在会去除裸“搜”及“在”被误识为“你”的前缀，正确保留 `彭瑞达的专利`。
- Google Patents 的检索词解析改为独立可测试模块；原始语音指令仍保持不变，只在本地构造跳转 URL 时派生检索词。

## v0.18.0

- 明确要求在 Google Patents 搜索时直接打开 `patents.google.com`，不再错误地先去 Bing。
- 从语音指令中清理“帮我/搜索/Google Patents”等控制词，仅把真正检索词传给专利站。
- GPT-6 单步规划增加 45 秒前端截止时间和 40 秒后端总预算，超时后给出可恢复终态，不再无限等待。

## v0.17.0

- 修复页面肉眼已加载但 Chrome 长期保持 `loading` 时任务无限等待的问题。
- 页面跳转期间主动探测网址、`readyState`、正文和操作元素，主要内容可用即继续。
- 对每次导航设置约 12 秒兜底；完整加载仍未结束时读取当前可见内容，不再永久卡住。
- 探测时校验页面 URL，避免误用跳转前遗留的旧 DOM。

## v0.16.0

- 新任务直接打开用户提供的网址或搜索结果页，省去空白页上的一次 GPT-6 规划。
- 发送给模型的元素保留 ID、标签和链接，但移除坐标、空字段及重复长文本，减少输入耗时。
- 浏览器右下角增加每秒更新的浮动状态，区分网页加载、模型分析、执行、重连和下载等待。
- 等待超过 20 秒时明确显示仍在处理而非卡死，并允许用户关闭浮层。

## v0.15.0

- 补充“看下/看看/查看论坛、社区、博客、文章、帖子、动态”等自然网页任务表达。
- 安装插件后，每条消息先由插件结合会话上下文做轻量意图判断，聊天页不再成为唯一入口。
- 普通聊天无需联网检查插件版本并会立即放行；只有网页任务才进入版本检查和浏览器操作。

## v0.14.0

- 浏览器任务上下文改为按聊天会话保存，切换会话或隔一段时间后仍能识别后续追问。
- 不含“搜索/打开”等关键词的短追问会先查询当前会话上下文，再决定是否交给插件。
- 热门列表收集到 8 条结构化结果后直接生成列表，省去通常最慢的最终模型调用。
- 进一步减少页面文字、元素和历史动作输入，并将普通任务硬上限降至 12 步。

## v0.13.0

- 每次用户触发浏览器任务时，插件都会实时查询服务器最新版，不再依赖刷新聊天页。
- 发现旧版时立即暂停任务，返回最新版号、下载地址和安装视频地址。
- 聊天页的更新卡片改为使用服务器动态版本信息，后续升级不再写死版本链接。

## v0.12.0

- 热门检索改为列表页批量收集，默认只核验 3 个重点结果和 1 个代码仓库。
- 收集到 8–10 个高质量结果后快速汇总，减少不必要的串行 GPT-6 决策。
- 页面文字、元素、历史结果和动作记录改为紧凑上下文，降低每轮分析耗时。
- 页面动作、跳转与断线恢复等待大幅缩短，同时保留复杂页面视觉兜底。

## v0.11.0

- 增加任务级规划锁与规划序号，阻止页面事件启动并行规划链，丢弃迟到的旧结果。
- DOM 可读时不再上传截图，仅在文字和元素不足时调用视觉模式。
- 页面指纹替代单纯网址判断，X.com 等单页应用内容变化后可继续操作。
- 普通重复动作自动重新规划，仅对重复下载保持硬拦截。
- 缩短页面动作等待时间，限制单步输出和输入规模，加快整体操作。

## v0.10.0

- “当前标签页没有可后退历史”不再终止任务，自动读取页面并规划其他路径。
- 页面元素变化、按钮暂时不可用等可恢复错误会自动换路径，连续失败才安全暂停。
- 失败原因写入最近动作，避免模型重复选择同一个无效动作。
- 研究任务遇到站点登录墙或访问限制时，可改用公开搜索页检索目标域名。

## v0.9.0

- 规划接口短暂失败或返回空结果时自动重试，不再直接显示“规划结果无效”。
- 连续重试仍失败时安全暂停，并提示用户稍后发送“继续”。
- 更新提示增加完整操作步骤，安装视频改为真实 Chrome 页面并标出点击位置。

## v0.8.0

- 修复网站打开新标签页后，旧任务状态覆盖新标签页编号的问题。
- 页面跳转造成内容脚本短暂断开时，自动等待并重新连接，不再立即终止任务。
- 重新连接后读取最新页面并重新规划，避免执行页面变化前的旧动作。
- 无法操作 Chrome 内置页或需要人工登录/验证时，给出明确的继续方法。

## v0.7.0

- 增加专用下载动作和 Chrome 下载事件检测。
- 下载开始后立即结束本轮任务，避免反复点击。
- 拦截同一任务中相同页面、相同目标的重复动作。
- 支持按用户要求安全重命名下载文件。
- 聊天页主动检测最新版，旧版本暂停执行并提示更新。
- 保留跨页面结果记忆与多轮追问能力。

## v0.6.0

- 增加跨页面研究账本，支持收集、去重和汇总多个页面的结果。

## v0.5.0

- 通用任务改为由 GPT-6 理解自然语言，不再依赖固定网站字段解析。

## v0.4.0

- 支持在同一会话中继续追问和修改任务条件。

## v0.3.0

- 聊天页与扩展采用单一执行链，避免同一请求被重复处理。
