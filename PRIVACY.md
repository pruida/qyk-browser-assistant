# 隐私与安全说明

启源浏览器助手用于把用户在 `ai.qykaiwu.cn` 发出的明确指令转换为当前 Chrome 中的受控网页操作。

## 会处理的信息

执行浏览器任务时，扩展可能处理当前目标页面的地址、标题、可见文字、可操作元素信息和页面截图，并将完成任务所需的信息提交给启源 AI 的规划接口。

## 不应由扩展处理的信息

扩展会阻止自动填写密码、短信验证码、身份证、护照、银行卡、信用卡安全码等敏感字段。遇到登录验证、付款、转账、最终下单、删除账户等关键步骤时，会暂停并要求用户本人操作。

## 浏览器权限用途

- `tabs`、`activeTab`：打开、切换和读取任务相关标签页。
- `storage`：在本机保存当前任务状态，支持页面跳转和多轮追问。
- `scripting`：在用户要求操作的普通网页中加载执行器。
- `downloads`：启动一次明确要求的下载，并按用户要求设置安全文件名。
- `<all_urls>`：允许插件在用户指定的不同 HTTP/HTTPS 网站工作，而不是只支持固定网站。

## 用户控制

用户可以随时在聊天中发送“停止浏览器操作”，也可以在 `chrome://extensions/` 中停用或删除扩展。删除扩展会清除其本地存储的任务状态，但不会自动删除已经下载的文件。

## 使用提醒

不要在公开的问题反馈中提交密码、验证码、证件号码、支付信息或包含隐私的完整页面截图。第三方网站仍受其自身的服务条款和隐私政策约束。
